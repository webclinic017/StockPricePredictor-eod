version: '3'
services:
  INTCpredictions: # name of project
    build:
      context: .                  #from where project files are taken and loaded
      dockerfile: Dockerfile.dev
    image: intel_prediction
    container_name: intc
    ports:
      - '4000:80'
    volumes:
      - .:/app
